# =============================================================================
# RVAMS Settings
# =============================================================================

[database]
# database_connection [Mandatory]: This is variable which can be used to point 
#the database so that hebe can reach.
path = "hebe.db"


# =============================================================================
# Slack Integration
# =============================================================================

[slack]
# webhook_endpoint = "https://hooks.slack.com/workflows/XYZ/ABC/123"
